name:df/1
def:df(module()) -> df_ret()

name:df/2
def:df(module(), atom()) -> df_ret()

name:df/3
def:df(module(), atom(), arity()) -> df_ret()

name:is_term_seen/2
def:is_term_seen(term(), [term()]) -> boolean()

name:size/1
def:size(term()) -> non_neg_integer()

